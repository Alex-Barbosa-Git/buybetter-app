name: buybetter
description: Comparador inteligente com georreferenciamento colaborativo.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.7
  geolocator: ^12.0.0
  flutter_dotenv: ^5.1.0
  intl: ^0.19.0
  http: ^1.2.2
  mobile_scanner: ^3.0.0
  flutter_map: ^7.0.1
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
